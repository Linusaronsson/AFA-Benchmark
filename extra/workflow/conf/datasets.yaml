# Which datasets we run the pipeline on
datasets:
  - afa_context
  - afa_context_without_noise
  # - bank_marketing # needs normalization
  # - ckd # needs normalization
  - cube
  - cube_without_noise
  # - diabetes # maybe needs normalization?
  # - fashion_mnist # data generation needs to be fixed
  # - imagenette # data generation needs to be fixed
  # - miniboone # needs normalization
  # - mnist # data generation needs to be fixed
  # - physionet # needs normalization
  - synthetic_mnist
  - synthetic_mnist_without_noise
