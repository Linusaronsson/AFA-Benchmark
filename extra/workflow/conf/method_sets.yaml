# Maps to methods in `methods.yaml`
method_sets:
  rl:
    - jafa
    - odin_model_free
    - odin_model_based
    - ol_without_mask
    - ol_with_mask
  dummy:
    - random_dummy
    - sequential_dummy
  odin_variants:
    - odin_model_free
    - odin_model_based
  ol_variants:
    - ol_without_mask
    - ol_with_mask
  # A subset of methods, as the main result
  selection:
    - jafa
    - odin_model_based
    - ol_without_mask
