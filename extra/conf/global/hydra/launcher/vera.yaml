# Vera CPU partition. GPU work uses `custom_slurm`.
defaults:
    - submitit_slurm

submitit_folder: ${hydra.sweep.dir}/.submitit/%j
timeout_min: 1440 # 24 hours; partition cap is 7 days

partition: cpu
account: C3SE2026-1-12

additional_parameters:
    cpus_per_task: 8
