hydra:
  searchpath:
    - file://extra/conf
    - file://extra/conf/global

defaults:
  - /components/masked_pretraining@_global_: default
  - /components/masking_probabilities@_global_: simple
  - optional experiment@_global_: ???
  - override hydra/job_logging: colorlog
  - override hydra/hydra_logging: colorlog
  - override hydra/launcher: custom_slurm
  - _self_

train_dataset_bundle_path: ???
val_dataset_bundle_path: ???
save_path: ???
device: cuda

# batch size set to shim2018 paper default, since kachuee2019 does not specify it

lr: 1e-3
pq_module:
  n_hiddens: [64,32,16] # kachuee2019 implementation for Diabetes
  p_dropout: 0.5 # kachuee2019 implementation

seed: null
use_wandb: false
smoke_test: false
