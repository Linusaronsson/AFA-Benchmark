defaults:
  - override /components/masking_probabilities@_global_: complex
# TEMP: check if pretrained model can achieve 100% accuracy with these params
encoder:
  output_size: 64
  reading_block_cells: [128, 128]
  writing_block_cells: [128, 128]
  memory_size: 64
classifier:
  num_cells: [128, 128] # shim2018 paper uses [32, 32] for the cube dataset, but that's not complex enough for MNIST.
