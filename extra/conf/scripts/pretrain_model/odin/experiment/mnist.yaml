defaults:
  - override /components/masking_probabilities@_global_: complex
classifier:
  num_cells: [128, 128] # odin paper does not say what this should be, so we use the same num_cells as the "common" classifier
